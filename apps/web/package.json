{
  "name": "@kaataan/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc -b && vite build",
    "test": "node --experimental-strip-types --test src/game/*.test.ts src/multiplayer/*.test.ts",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@kaataan/game-engine": "workspace:*",
    "@kaataan/protocol": "workspace:*",
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest"
  }
}
